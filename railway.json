{
  "build": {
    "command": "npm install && npx prisma generate --schema=src/prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "npx prisma db push --schema=src/prisma/schema.prisma --skip-generate && npm start",
    "healthcheckPath": "/",
    "restartPolicyType": "on_failure"
  }
}